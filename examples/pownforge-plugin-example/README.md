# pownforge-plugin-example

Minimal external PownForge plugin built on `pownforge.sdk`
(see `docs/handbook.md` §6 "外部プラグイン(Plugin SDK)").

```bash
pip install -e examples/pownforge-plugin-example
pownforge plugin info http-title
pownforge scan run http-title --target <registered url target>
```
